# ghostty

| Committed file | Symlinked to               | Purpose                 |
|----------------|----------------------------|-------------------------|
| `config`       | `~/.config/ghostty/config` | Ghostty terminal config |

Edit `config` and reload Ghostty.
